name: Deploy Agama Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: agama-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout Agama
        uses: actions/checkout@v4

      - name: Verify standalone webbook source
        shell: bash
        run: |
          set -euo pipefail
          test -f index.html
          test -f reader.html
          test -f reader.js
          test -f style.css
          test -f app.js
          test -f volumes.js
          grep -q '제1~11권' index.html
          grep -q '\[11,273,282,10,"published"' volumes.js
          for n in 01 02 03 04 05 06 11; do test -f "volumes/$n.md"; done
          test -f volumes/07.seg1.mdpart
          test -f volumes/08.seg1.mdpart
          test -f volumes/09.seg1.mdpart
          test -f volumes/10.seg1.mdpart
          echo 'Agama source verification passed.'

      - name: Stage Pages artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf dist
          mkdir -p dist
          cp index.html reader.html reader.js style.css app.js volumes.js dist/
          cp -R volumes dist/
          printf 'agama-pages-ok\n' > dist/health.txt
          touch dist/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Verify published Agama site
        shell: bash
        env:
          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          set -euo pipefail
          base="${PAGE_URL%/}/"
          ok=0
          for i in $(seq 1 12); do
            if curl -fsSL --max-time 20 "${base}health.txt" | grep -q 'agama-pages-ok' \
              && curl -fsSL --max-time 20 "${base}" | grep -q '제1~11권' \
              && curl -fsSL --max-time 20 "${base}reader.html?vol=11" | grep -q '잡아함경 쉬운 완전읽기' \
              && curl -fsSL --max-time 20 "${base}volumes/11.md" | grep -q '제11권'; then
              ok=1
              break
            fi
            sleep 5
          done
          test "$ok" -eq 1
          echo "Verified live site: ${base}"
